1) SQLite backend for database. Implement backend update scheduling for archiving events
2) Submission validation on front and backend. Think about what we are storing and how (time should be a time obj not a string)
3) Pretty frontend using React. Components wanted (map, two pages for view and submission)
